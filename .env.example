# Copy this file to .env and fill in real values.
# .env is gitignored and never committed.

APP_NAME=OmniAssist AI
ENVIRONMENT=development

STORAGE_DIR=storage
SQLITE_PATH=storage/omniassist.db

API_KEY=changeme-dev-key
